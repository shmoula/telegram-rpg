services:
  telegram-rpg:
    build: .
    restart: unless-stopped
    mem_limit: 1g
    env_file: .env
    volumes:
      - ./bot_data:/data
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "3"